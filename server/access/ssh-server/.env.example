# SEIS SSH server configuration (copy to .env — .env is gitignored)
# No secrets here; only non-sensitive runtime config.

# Login user created inside the server
SSH_USER=seis
SSH_UID=2000

# Host port mapped to the container's :2222
SSH_HOST_PORT=2222
