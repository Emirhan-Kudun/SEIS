# Core

- Project root: `/Users/emirhan/Library/Mobile Documents/com~apple~CloudDocs/Github/UIX-Apps`.
- Active governance branch for plugin/source work: `codex/requested-plugin-governance-20260602`; preserve unrelated dirty files before staging.
- Source-visible environment model lives in generated content/report pairs plus `deploy/cloud-environment.json`.
- Plugin-heavy user requests route to bounded, auditable source-chain artifacts unless a concrete live connector task with auth/scope is requested.
- Read `mem:tech_stack` for runtime and script shape, `mem:conventions` for generator/checker rules, and `mem:task_completion` before finishing code changes.