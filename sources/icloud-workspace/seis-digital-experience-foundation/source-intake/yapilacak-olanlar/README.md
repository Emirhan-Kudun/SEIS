# Yapilacak Olanlar Source Intake

This folder preserves the standalone Dreamweaver-compatible portfolio source from:

`/Users/emirhan/Downloads/Yapılacak olanlar/files`

## Files

- `index.html`: split-file portfolio page.
- `style.css`: split-file visual system.
- `script.js`: split-file interaction layer.
- `emirhan-kudun-portfolio.html`: single-file HTML portfolio reference.

## Promotion Policy

These files are reference material, not the active root runtime. Useful design and interaction ideas should be promoted into the canonical surfaces through reviewed changes:

- root static portfolio: `index.html`, `style.css`, `script.js`
- full-stack UX app module: `apps/ux-apps-foundation/`
- infrastructure contracts: `config/`, `data/`, `docs/`, `scripts/`

The source intake must not include `.git`, `.DS_Store`, zip files, local runtime files, or dependency folders.
